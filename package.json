{
  "name": "codequest-js",
  "private": true,
  "workspaces": [
    "backend",
    "io",
    "mobile",
    "packages/*"
  ],
  "scripts": {
    "postinstall": "patch-package",
    "db:generate": "npm --prefix packages/db run prisma:generate",
    "backend:dev": "npm --prefix backend run dev",
    "backend:build": "npm --prefix backend run build",
    "io:dev": "npm --prefix io run dev",
    "io:build": "npm --prefix io run build",
    "mobile:start": "npm --prefix mobile run start",
    "typecheck:all": "npm --prefix packages/streak-logic run typecheck && npm --prefix packages/xp-constants run typecheck && npm --prefix packages/duel-constants run typecheck && npm --prefix packages/exercise-answer run typecheck && npm --prefix packages/user-credentials run typecheck && npm --prefix packages/db run typecheck && npm --prefix backend run typecheck && npm --prefix io run typecheck && npm --prefix mobile run typecheck && npm --prefix packages/auth-jwt run typecheck && npm --prefix packages/server-kit run typecheck",
    "android": "npm --prefix mobile run android",
    "ios": "npm --prefix mobile run ios",
    "ios:device": "npm --prefix mobile run ios:device"
  },
  "devDependencies": {
    "patch-package": "^8.0.1"
  },
  "version": "1.0.0",
  "dependencies": {
    "expo": "~54.0.33",
    "react": "19.1.0",
    "react-native": "0.81.5"
  }
}
